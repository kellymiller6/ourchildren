import { receiveData } from '../../firebase/Firebase.js'

export const receiveSitters = (sitters) => {
  return {
    type: 'GET_SITTERS',
    quizzes
  }
}

export const getSitters = (sitters) => {
  return dispatch => {
    receiveData()
  }
}
